import numpy as np

time = np.array([0, 1, 2, 3, 4])
height = np.array([0, 5, 12, 15, 8])

total_displacement = height[-1] - height[0]
total_time = time[-1] - time[0]

average_velocity = total_displacement / total_time

print("Average Velocity:", average_velocity)
